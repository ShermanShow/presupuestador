import AdminVendedores from "@/components/AdminVendedores";

export const metadata = { title: "Administración — Presupuestador" };

export default function AdminPage() {
  return <AdminVendedores />;
}
