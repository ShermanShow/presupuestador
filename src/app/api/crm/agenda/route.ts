import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
const pass = () => process.env.ADMIN_PASSWORD || "1234";
function db() { const url = process.env.SUPABASE_URL; const key = process.env.SUPABASE_SERVICE_ROLE_KEY; if (!url || !key) throw new Error("Supabase no configurado"); return createClient(url, key, { auth: { persistSession: false } }); }
export async function GET(request: NextRequest) { if (request.headers.get("x-admin-password") !== pass()) return NextResponse.json({ error: "Clave incorrecta" }, { status: 401 }); const id = new URL(request.url).searchParams.get("cliente_id"); const { data, error } = await db().from("cliente_agenda").select("*").eq("cliente_id", id).order("fecha_inicio"); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ agenda: data || [] }); }
export async function POST(request: NextRequest) { if (request.headers.get("x-admin-password") !== pass()) return NextResponse.json({ error: "Clave incorrecta" }, { status: 401 }); const body = await request.json(); if (!body.cliente_id || !body.fecha_inicio || !String(body.accion || "").trim()) return NextResponse.json({ error: "Fecha y acción son obligatorias" }, { status: 400 }); const { data, error } = await db().from("cliente_agenda").insert({ cliente_id: body.cliente_id, fecha_inicio: body.fecha_inicio, duracion_min: Number(body.duracion_min) || 30, accion: body.accion, estado: "Pendiente" }).select("*").single(); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ evento: data }, { status: 201 }); }
export async function PATCH(request: NextRequest) { if (request.headers.get("x-admin-password") !== pass()) return NextResponse.json({ error: "Clave incorrecta" }, { status: 401 }); const body = await request.json(); const { data, error } = await db().from("cliente_agenda").update({ estado: body.estado, accion: body.accion, fecha_inicio: body.fecha_inicio, duracion_min: body.duracion_min, updated_at: new Date().toISOString() }).eq("id", body.id).select("*").single(); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); return NextResponse.json({ evento: data }); }
